== Django backend for easy authentication with Facebook == 



1) Create an app on facebook and activate sandbox mode - this will let you use http requests - and change canvas URL to http://127.0.0.1:8000/login/
2) Change everything on your settings.py that is inside <> to your configuration

TEMPLATE_DIRS = (
    '<YOUR-DJANGO-PROJECT>/django_fukinbook/templates',
)

AUTHENTICATION_BACKENDS = (
    '<YOUR-DJANGO-PROJECT>.django_fukinbook.backends.FacebookBackend',
    'django.contrib.auth.backends.ModelBackend',
)

INSTALLED_APPS = (
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.sites',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'django_fukinbook',
    'django.contrib.admin',
)

FACEBOOK_APP_ID = '<YOUR-APP-ID>'
FACEBOOK_APP_SECRET = '<YOUR-APP-SECRET>'
FACEBOOK_REDIRECT_URI = 'http://127.0.0.1:8000/login/' # NOTE IT IS NOT localhost:8000 - It's because I put 127.0.0.1:8000 on my facebook application URL
FACEBOOK_APP_SCOPE = ','.join(['user_relationships', 
                               'friends_birthday']) # You can change to facebook scopes you want to use

GRAPH_API_URL = 'https://graph.facebook.com/'

3) Change your urls.py

from django.conf.urls.defaults import patterns, include, url
from django.contrib import admin
admin.autodiscover()

urlpatterns = patterns('',
    url(r'^login/', 'django_fukinbook.views.login'),
    url(r'^canvas/', 'django_fukinbook.views.canvas'),
    url(r'^admin/', include(admin.site.urls)),
)

4) Change your loggers
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'simple': {
            'format': '%(asctime)s [%(levelname)s] - %(module)s: %(message)s'
        },
    },
    'handlers': {
        'mail_admins': {
            'level': 'ERROR',
            'class': 'django.utils.log.AdminEmailHandler'
        },
        'console':{
            'level':'DEBUG',
            'class':'logging.StreamHandler',
            'formatter': 'simple'
        },
    },
    'loggers': {
        'fukinbook.custom': {
            'handlers': ['console'],
            'level': 'DEBUG',
            'propagate': True,
        },
        'django.request': {
            'handlers': ['mail_admins'],
            'level': 'ERROR',
            'propagate': False,
        },
    }
}

import logging
LOGGER = logging.getLogger('fukinbook.custom')


5) python manage.py syncdb
6) Go to browser and type http://127.0.0.1/login/

We started this project from http://djangosnippets.org/snippets/2065/
Thanks to http://djangosnippets.org/users/barnardo/
